{
    "version": 2,
    "name": "tcc-code-network-api",
    "builds": [
        {
            "src": "src/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/(.*)"
        }
    ]
}